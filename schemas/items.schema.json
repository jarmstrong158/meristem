{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "meristem:items",
  "title": "Items and gear",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "rarity_tiers": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
          "name": { "type": "string", "minLength": 1 },
          "weight": { "type": "number", "minimum": 0 }
        }
      }
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "slot"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
          "name": { "type": "string", "minLength": 1 },
          "slot": { "enum": ["weapon", "armor", "accessory", "consumable", "key_item"] },
          "rarity": { "type": "string", "description": "references a rarity_tiers id" },
          "stats": { "type": "object", "additionalProperties": { "type": "number" } },
          "sprite": { "$ref": "#/$defs/sprite" }
        }
      }
    },
    "drop_tables": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["enemy_id", "drops"],
        "properties": {
          "enemy_id": { "type": "string", "description": "references entities.enemies id" },
          "drops": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["item_id", "weight"],
              "properties": {
                "item_id": { "type": "string", "description": "references items id" },
                "weight": { "type": "number", "exclusiveMinimum": 0 }
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "sprite": {
      "description": "A spec-addressable sprite: a generator archetype + its config (dec-0022).",
      "type": "object",
      "additionalProperties": false,
      "required": ["archetype"],
      "properties": {
        "archetype": { "enum": ["humanoid", "blob", "ghost", "quadruped", "flyer", "serpent", "spider", "weapon", "consumable", "pickup", "chest", "projectile", "tile"] },
        "config": { "type": "object" }
      }
    }
  }
}
